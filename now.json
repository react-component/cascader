{
  "version": 2,
  "name": "rc-cascader",
  "builds": [
    {
      "src": "package.json",
      "use": "@now/static-build",
      "config": { "distDir": ".doc" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/.doc/$1" }
  ]
}
